#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        while(n--)
        {
            string a;
            cin>>a;
            if(a[0]=='d' || a[0]=='f')
                cout<<"yes";
        }
    }
    return 0;
}
