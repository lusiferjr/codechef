/*#include<bits/stdc++.h>
#define in(x) cin>>x;
#define lo while(t--)
#define prn(x) cout<<x;
#define nl <<"\n";
#define pr cout<<
#define fr(i,n,x) for(int i=x;i<n+x;i++)
#define ll long long
#define mod(x) x%=1000000007;
 using namespace std;
 unsigned ll fact (unsigned ll a)
 {

 }
 int main()
 {
     int t;
     in(t)
     lo
     {
        int n;
         in(n)
        unsigned ll x=1;
         fr(i,n-1,2)
         {
            x=x+i+(x*i);
            mod(x)
         }
         prn("\n")
            pr x nl
      }
     return 0;
 }*/
 /*unsigned long long factorial(int n)
{
    const unsigned int M = 1000000007;

    unsigned long long f = 1;
    for (int i = 1; i <= n; i++)
        f = (f*i) % M;  // Now f never can
                        // exceed 10^9+7
    return f;
} */
