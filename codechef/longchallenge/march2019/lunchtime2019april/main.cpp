/*#include<bits/stdc++.h>
#define in(x) cin>>x;
#define lo while(t--)
#define prn(x) cout<<x;
#define nl <<"\n";
#define pr cout<<
#define fr(i,n,x) for(int i=x;i<n+x;i++)
#define ll long long
using namespace std;

int main()
{
    int t;
    in(t)
    lo
    {
        int n;
        in(n)
         string name[n][2];
          fr(i,n,0)
           {
               string a;
               in(a)
               name[i][0]=a;
               in(a)
               name[i][int x;
            in(x)
            a[i]=x;
            suma+=x1]=a;
           }
           fr(i,n,0)
             {
                int flag=0;
                 for(int j=0;j<n;j++)
                 {
                     if(i!=j){
                     {if(name[i][0]==name[j][0])
                        flag=1;}}
                 }
                 if(flag==1)
                    pr name[i][0]<<" "<<name[i][1] nl
                 else
                    pr name[i][0] nl
             }

    }
    return 0;
}*/
